export interface Credentials {
  seedToken: string;
  accessToken: string;
  accountNumber: string | number;
  apiServer: string;
  apiUrl: string;
  apiVersion: string;
  authUrl: string;
  expiresIn: number;
  keyDir: string;
  keyFile: string;
  practice: boolean;
  refreshToken: string;
  tokenType: string;
  qtMarketsNames: any;
}
