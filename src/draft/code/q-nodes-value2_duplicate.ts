// -----------------------------------------------------------------------------!!

export interface QNodesValue2_duplicate {
  cb?: any;
  config: any;
  fn: <R>(config?: unknown) => R;
  functionKind?: 'other';
}
